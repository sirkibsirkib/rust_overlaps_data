Each folder corresponds with a setup for the system.
Each file within corresponds with a single run of the system using that setup, and the name of the .out file is suggestive of the data set used.
Each line represents a filter length for the run, with columns for various measurements